<%= form_tag foods_path, method: :get %>
<%= label_tag "Search Foods" %>
<%= text_field_tag :query %>
<p><% @foods.each do |food| %>
<p><%= link_to "#{food.name}", food_path(food.id) %>

 <% end %></p>



<%= button_to "Create a Food", new_food_path, method: 'get' %>